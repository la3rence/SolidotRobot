name: cron

on:
  schedule:
  - cron: '*/10 * * * *'

jobs:
  triggers:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
    - run: date && curl -XGET https://solibot.vercel.app/api/start
